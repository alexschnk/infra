services:
  dockhand:
    image: fnsys/dockhand:latest
    container_name: dockhand
    restart: unless-stopped
    ports:
      - 8081:3000
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - /home/alex/data/dockhand/dockhand_data:/app/data
