http:
 #region routers 
  routers:
    victoria-metrics:
      entryPoints:
        - "https"
      rule: "Host(`victoria-metrics.homelab.ru`)"        # Change DOMAIN_NAME to yours
      middlewares:
        - security-headers@file
        - authelia@docker
      tls: {}
      service: victoria-metrics

  services:
    victoria-metrics:
      loadBalancer:
        servers:
          - url: "http://192.168.31.204:8085"            # Change IP Address to your proxmox instance
        passHostHeader: true
